services:
  - type: web
    name: bing-news-rss-proxy
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:10000 --workers 2 --timeout 30
    envVars:
      - key: PORT
        value: "10000"
    plan: free
